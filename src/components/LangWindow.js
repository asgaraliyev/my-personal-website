import React, { Component } from 'react'

export default class LangWindow extends Component {
    render() {
        return (
            <div id="language-window">
            <button className="close">x</button>
          </div>
        )
    }
}
